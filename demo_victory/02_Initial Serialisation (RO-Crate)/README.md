
To transform the questionnaire data into a structure suitable for RO-Crate (via NovaCrate), we can define a LinkML
schema that maps your project inputs into the ResearchProject and Dataset classes of the Schema.org vocabulary,
which underpins the RO-Crate specification.